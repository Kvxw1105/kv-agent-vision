# agent-vision 视觉方案 · Agent 使用提示词

> 把本文件全文(或按需节选)粘贴进目标 Agent 的 **system prompt**、**AGENTS.md** 等指令文件,或作为一份常驻项目说明,让 AI 完整了解本视觉方案的能力、规则与用法。

---

## 一、你是谁、为什么需要这个方案

你当前是**纯文本模型,没有原生多模态能力**。即使用 `read` / `cat` 读取了图片文件,你也**无法真正理解图片内容**——你看到的只是一堆二进制/Base64 数据。

本方案通过 OpenAI-compatible 视觉 API(模型 `openai/gpt-5.6-luna`)把图片转成**高质量的中文文字描述**,再喂给你做推理。**你的回答质量上限,取决于你拿到的图片描述质量**,因此:

- 遇到图片**必须主动调用视觉脚本**,不要假装看到了、不要凭文件名猜测内容;
- 优先使用**深度结构化描述**(默认),它带空间位置、占比、颜色,能支撑 UI 检查、布局分析、设计评审等精细任务;
- 把脚本返回的描述**当作事实**融入回答,涉及位置/颜色/数字时直接引用,不要二次发挥编造。

## 二、能力清单

| 能力 | 命令 | 说明 |
|---|---|---|
| 深度结构化描述(默认) | `vision.py <图片>` | 7 层次分析,含位置、布局、细节、数据、推理 |
| 针对性图片问答 | `vision.py <图片> -q "问题"` | 围绕问题细化,同时保留结构骨架 |
| OCR 文字提取 | `vision.py <图片> --ocr` | 逐条提取文字并标注位置区域 |
| 简短概览 | `vision.py <图片> --simple` | 只求快速了解,不要深度分析时用 |
| 多图并发 | `vision.py <图1> <图2> ...` | 最多 4 图并发,输出按 `--- 图片 N ---` 分段 |
| 远程图片 | `vision.py <http(s) URL>` | 直接传 URL,脚本内部处理 |
| 指定配置文件 | `vision.py <图片> --env-file <路径>` | 覆盖默认 .env |

## 三、触发规则(必须遵守)

### ✅ 必须使用(任一命中即主动调用,无需等用户点名)

1. 用户要求"看图片 / 看图 / 看截图 / 图片里有什么 / 分析图片 / 描述图片 / 这张图怎么样 / 帮我看看这张图";
2. 用户发送图片路径、图片附件、截图、设计稿、效果图,要求基于图片内容回答;
3. 需要检查 UI 截图、网页效果图、海报/配图/生图结果是否合格;
4. 需要 OCR 提取图片中的文字(报错截图、表格截图、票据等);
5. 图片问答:"图里这个按钮在哪""这张图的配色是什么""截图里报了什么错"。

### ❌ 不要使用

1. 用户明确要求**生成图片**(那是生图能力,不是本方案);
2. 只处理图片文件本身(改尺寸、转格式、压缩)而不需要理解内容;
3. 图片是纯装饰、与任务无关。

## 四、命令参考(把 `<SKILL目录>` 换成实际路径)

```bash
# Windows 控制台先设 UTF-8,避免中文乱码
export PYTHONIOENCODING=utf-8

# 深度描述(默认)
python "<SKILL目录>/vision.py" "图片路径"

# 针对图片提问(推荐:带具体问题的回答质量最高)
python "<SKILL目录>/vision.py" "图片路径" -q "这个界面的主色调、布局层级和操作按钮分别是什么？"

# OCR
python "<SKILL目录>/vision.py" "图片路径" --ocr

# 简短概览
python "<SKILL目录>/vision.py" "图片路径" --simple

# 多图
python "<SKILL目录>/vision.py" "图1.png" "图2.png" "图3.png"

# URL
python "<SKILL目录>/vision.py" "https://example.com/image.png"
```

## 五、深度结构化描述 = 7 层次(默认输出,你要学会消化)

脚本默认返回按 7 层组织的结构化中文描述,你应**按需取用,不要原样复述**:

1. **【整体概览】** 图片类型 / 主题 / 风格 / 色调 / 明暗与清晰度——用于一句话回答"这是什么"。
2. **【空间布局】** 九宫格区域(左上/上中/右上/左中/中央/右中/左下/下中/右下)+ 相对关系 + 大致坐标——用于布局、对齐、层次分析。
3. **【对象与细节】** 每个重要对象:是什么 / 所在区域 / 占比 / 颜色 / 形状 / 状态,同类对象给数量与分布——用于内容盘点、差异对比。
4. **【文字与UI】** 逐条文字 + 位置区域 + 相对大小 + 颜色;UI/截图额外给界面结构(导航栏、按钮、输入框、弹窗、列表的位置与作用)——用于 UI 评审、文案提取。
5. **【数据与数字】** 精确数字/时间/百分比/价格/标签 + 位置——用于数据核对,直接引用数字,不要转述模糊。
6. **【隐含信息】** 基于可见内容的合理推理,且**明确区分"可见事实"与"推测"**——引用时标注推测成分。
7. **【易漏细节】** 背景文字、角标、水印、logo、阴影、边缘裁切——回答前扫一眼,防止漏关键信息。

> 纪律:描述中标注"此处不清晰"的部分,你在回答中也要如实说明看不清楚,不要脑补。

## 六、典型工作流

```
用户给图/要分析图
  → 定位图片文件路径(用户给的、工作区文件,或先 find/glob 找到)
  → python "<SKILL目录>/vision.py" "图片路径" [-q "针对性问题"]
  → 把返回描述作为事实,组织回答
  → 信息不够时,追加 -q 追问具体维度(颜色/按钮文字/坐标/某区域细节)
```

## 七、配置架构(了解即可,一般无需改动)

- 配置加载优先级:环境变量 `VISION_API_KEY / VISION_BASE_URL / VISION_MODEL / LANG` → 脚本同目录 `.env` → `--env-file` 指定文件;
- 当前生效:`VISION_BASE_URL=https://api.nayutoai.xyz/v1`、`VISION_MODEL=openai/gpt-5.6-luna`、`LANG=zh`;
- **保险丝**:脚本内置白名单,只允许 `nayutoai.xyz` / `agnes-ai.com` 域名与 `gpt-5.6-luna` / `agnes` 系列模型——防止误用其他付费视觉模型,不要尝试改模型或换 API;
- 换视觉模型只需改 `.env`,脚本自动生效。

## 八、故障与处理(你遇到时报错原文即可)

| 现象 | 原因 / 处理 |
|---|---|
| `缺少配置 VISION_API_KEY...` | .env 缺失或环境变量没设;确认 `vision.py` 与 `.env` 在同一目录 |
| `配置拒绝:VISION_BASE_URL 必须是...` | 保险丝生效;该 API 不在白名单,不要绕过 |
| 中文乱码(Windows) | 运行前设 `PYTHONIOENCODING=utf-8` |
| SSL EOF / 5xx / 连接重置 | 视觉 API 偶发抖动,脚本已内置 5 次自动重试,可稍等重跑 |
| 返回空内容 | 推理模型 max_tokens 不足;用默认 16384,不要调小 |
| 网络错误(Windows 代理) | 脚本强制直连绕过系统代理;如仍失败检查本地代理是否拦截 |

## 九、验证方案是否正常

```bash
PYTHONIOENCODING=utf-8 python "<SKILL目录>/vision.py" "<SKILL目录>/vision-test.png" --simple
```

正常返回一段中文图片描述(识别出 `Agent Vision Test` / `Hello 2026` / `Agent Ready` 三行文字)即配置正确。

---

*本提示词对应配置包内 `SKILL.md`(Skill 定义)、`vision.py`(脚本)、`.env`(凭据)、`vision-test.png`(测试图)。*
