---
name: agent-vision
description: 图片理解/看图能力（为纯文本模型提供视觉）。当用户要求"看图/看图片/看截图/看这张图/识别图片/图片里有什么/分析图片/描述图片/OCR/提取图片文字/图片问答/读取图片内容/看UI截图/看效果图/看设计稿"等任何涉及查看或理解图片内容的请求时使用。当前 Agent 为纯文本模型（无多模态能力），read 工具读取图片后无法直接理解图片内容，必须使用本 Skill 调用视觉 API 生成文字描述后再回答。这是默认看图能力，遇到图片理解需求主动使用，无需用户点名。版本 1.0.0。
version: 1.0.0
---

# agent-vision：图片理解（默认看图能力）

## 为什么存在

当前 Agent 是**纯文本模型，没有多模态能力**。即使通过 `read` 工具读取了图片文件，Agent 也无法真正理解图片内容。本 Skill 通过 OpenAI-compatible 视觉 API 把图片转成文字描述，让 Agent 获得"看图"能力。

**这是默认能力**：只要任务涉及理解图片内容，就主动使用本 Skill，不要等用户提醒。

## 何时使用

**必须使用**（以下任一场景）：
- 用户要求"看图片 / 看图 / 看截图 / 图片里有什么 / 分析图片 / 描述图片 / 这张图怎么样"
- 用户发送图片路径/附件，要求基于图片内容回答
- 需要检查 UI 截图、效果图、设计稿、报错截图、生成图片的结果
- 需要 OCR 提取图片中的文字
- 图片问答（"图里这个按钮在哪"）

**何时不要用**：
- 用户明确要生成图片（那是生图能力，不是本 Skill）
- 仅处理图片文件本身（改大小、转格式）而不需要理解内容

## 用法

```bash
# 基本描述（中文输出）
python "<SKILL目录>/vision.py" <图片路径>

# 针对图片提问
python "<SKILL目录>/vision.py" <图片路径> -q "这个界面的主色调是什么？"

# OCR 提取文字(带位置标注)
python "<SKILL目录>/vision.py" <图片路径> --ocr

# 简短模式(不启用深度描述,只要快速概览)
python "<SKILL目录>/vision.py" <图片路径> --simple

# 多图并发描述
python "<SKILL目录>/vision.py" <图1> <图2> <图3>

# 指定 env 配置文件（可选）
python "<SKILL目录>/vision.py" <图片路径> --env-file /path/to/env
```

也支持直接传 http(s) URL 或 data URL。

## 默认深度描述(强化版)

默认输出**深度结构化描述**,按 7 个层次组织:
1. 【整体概览】图片类型 / 主题 / 风格 / 色调 / 清晰度
2. 【空间布局】九宫格区域(左上/上中/右上/左中/中央/右中/左下/下中/右下)+ 相对关系 + 大致坐标
3. 【对象与细节】每个对象:是什么 / 所在区域 / 占比 / 颜色 / 形状 / 状态,同类对象说明数量与分布
4. 【文字与UI】逐条提取文字并标注位置区域、相对大小、颜色;UI/截图额外描述界面结构
5. 【数据与数字】精确读出数字并注明位置
6. 【隐含信息】合理视觉推理,明确区分"可见事实"与"推测"
7. 【易漏细节】背景文字、角标、水印、logo、边缘裁切等

这保证喂给下游主模型(如 DeepSeek)的是高质量、带空间信息的描述,直接决定后续推理质量。
`-q` 追问时围绕问题细化并保留结构骨架;`--simple` 退回简短描述;`--ocr` 输出带位置标注。

## Agent 工作流

1. 确认图片路径（用户给的路径、工作区文件、或先 `read`/`find` 定位图片文件）
2. 运行 `vision.py` 生成描述：
   ```bash
   python "<SKILL目录>/vision.py" "图片路径" -q "针对性的问题"
   ```
3. 把返回的描述作为事实，融入回答
4. 需要更详细时，追加 `-q` 追问具体细节（OCR、颜色、布局、按钮文字等）

## 配置（凭据）

脚本自动从以下位置加载视觉 API 配置（按优先级）：

1. 环境变量 `VISION_API_KEY` / `VISION_BASE_URL` / `VISION_MODEL` / `LANG`
2. 脚本同目录 `.env` 文件
3. Windows: `%LOCALAPPDATA%\codex-deepseek-vision\env`（与 Codex 视觉代理共用同一份配置）

推荐配置（2026-08-05 生效）：
- `VISION_BASE_URL`: https://api.nayutoai.xyz/v1
- `VISION_MODEL`: openai/gpt-5.6-luna
- `LANG`: zh

> 修改视觉模型时只需改 env 文件，本 Skill 自动生效。

## 验证

```bash
python "<SKILL目录>/vision.py" <任意本地图片路径>
```

正常返回一段中文图片描述即配置正确。

## 注意事项

- 视觉 API 调用**强制直连**（绕过系统代理），Windows 系统代理可能导致连接失败
- 视觉 API 连接偶发不稳定（SSL EOF / 5xx），脚本已内置 5 次自动重试
- 视觉 API 是推理模型时，`max_tokens` 要给足（默认 8192），太小会返回空内容
- 图片描述质量取决于视觉 API 模型；仅理解图片内容，不修改图片本身
